spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: true
    baseline-on-migrate: true
    baseline-version: 0
    validate-on-migrate: true
  session:
    redis:
      namespace: "realmoneyrealtaste:session"
    timeout: 30m
